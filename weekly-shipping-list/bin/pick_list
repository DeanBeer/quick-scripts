#!/usr/bin/env ruby

$:.unshift(File.join( File.dirname(__FILE__), '..'))
require 'nrb'

pick_list = NRB::PickList.new

ARGV.each do |arg|
  results = pick_list.parse str: File.read(arg)
  pick_list.tally results
end

pick_list.present
