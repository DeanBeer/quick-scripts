#!/usr/bin/env ruby

lib = File.expand_path '../../lib', __FILE__
$LOAD_PATH.unshift(lib) unless $LOAD_PATH.include?(lib)

require 'nrb/support'
require 'nrb/delivery_pick_list'

pick_list = NRB::DeliveryPickList.new

ARGV.each do |arg|
end
